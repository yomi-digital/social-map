spec:
  name: social-map
  static_sites:
  - name: web
    git:
      branch: main
      repo_clone_url: https://github.com/tuousername/social-map.git
    build_command: yarn install && yarn build
    output_dir: dist
    environment_slug: node-js
    envs:
    - key: NODE_VERSION
      value: 18 